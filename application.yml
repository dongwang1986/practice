my:
  list:
    values: One,Two,Three,4

db:
  connection: "{connectionString:'http://localhost', userName:'Dong', password:'admin'}"
  host: 127.0.0.1
  port: 1200

management:
  endpoints:
    web.exposure:
      include: "*"

spring.profiles.active: atest
